
## 二、GameMode — 工具系统

> 详细设计文档：`docs/GameMode/工具基建设计.md`

### 已完成

- [x] `dice`：骰子检定（NdM+X、难度判定、patchByOutcome）
- [x] `table`：随机表（权重、条件过滤、tags、subrolls、模板渲染）
- [x] `encounter`：组合式随机遭遇（多池抽取、actor traits 三模式、模板渲染）
- [x] `stateCheck`：状态检查（path/op/value 条件比对）
- [x] `patchState`：安全状态修改（allowInputPatch、allowedPaths 路径过滤）
- [x] 工具 visibility 权限控制（ai / manual / trigger）
- [x] AI 工具闭环（Tool Loop）：maxToolRounds=3, maxToolCalls=8, forceFinal
- [x] 统一 ToolResult 格式

### 待完成

- [ ] 新工具类型：`choice`（分支选择工具，让玩家在 AI 提供的选项中做选择）
- [ ] 工具执行超时保护（单个工具执行时间上限）

---

## 三、GameMode — 随机系统

> 详细设计文档：`docs/GameMode/随机池与随机表.md`

### 第一阶段 ✅ 已完成

- [x] `pool` 数据结构
- [x] `table` 支持 `condition`、`tags`、`subrolls`
- [x] `encounter` 工具支持 `rolls` 和 `template`
- [x] `encounter.actor` 支持内联 `traits`（override / merge / extra 三模式）
- [x] 统一 `ToolResult.data.rolls`
- [x] 加权随机（`pickWeightedEntry()`）
- [x] 条件过滤后抽取（`evaluateConditions()` + `filterEntries()`）
- [x] 标签匹配（`matchTagsFrom`）
- [x] `tagsFromState`（从状态路径读取标签做偏好过滤）
- [x] 子词条（`subrolls`，递归支持 max depth=6）
- [x] 模板渲染（`{{path.to.value|fallback}}` 语法，支持数组展开）
- [x] 词条 `patch` 合并

### 第二阶段 ✅ 已完成

- [x] `chance` 概率跳过
- [x] `count`（含 `{min, max}` 随机范围，`normalizeCount()`）
- [x] 不放回抽取（`replacement: false`）

---

## 四、GameMode — 触发器

> 详细设计文档：`docs/GameMode/触发器.md`

### 已完成

- [x] 条件检查（全部比较操作符：eq/neq/gt/gte/lt/lte/truthy/falsy/includes/every）
- [x] `once`（只触发一次）
- [x] `maxRuns`（最大触发次数）
- [x] `cooldownTurns`（冷却回合数）
- [x] `triggerState` 持久化（runCount, lastRunTurn）
- [x] 触发器→工具执行→结果回传 AI 完整闭环

### 待完成

- [ ] 触发器优先级（多触发器同时满足时的执行排序）
- [ ] 触发器组合条件（AND/OR 逻辑组合）

---

## 五、GameMode — 提示词系统

> 详细设计文档：`docs/GameMode/提示词构造.md`

### 已完成

- [x] 三层模型（前缀层/对话层/后缀层）
- [x] `buildGamePrefixMessage()`：前缀缓存优化（不变内容前置）
- [x] `buildGameTurnSuffixMessage()`：每轮变化内容后置
- [x] 前缀缓存命中率约 96%（20 轮测算）

### 待完成

- [ ] 双模型策略探索（toolPlanner 用快模型，narrator 用创作模型）
- [x] 机制包升级后提示词直接切换到新模板（当前策略：不做平滑过渡）

---

## 六、GameMode — 机制包

> 详细设计文档：`docs/GameMode/机制包结构.md`、`docs/GameMode/机制包制作指南.md`

### 已完成

- [x] 声明式 JSON 机制包格式
- [x] `normalizeGamePack()`：完整归一化
- [x] 内置包管理（`getAllBuiltinGamePacks()`）
- [x] 自定义包管理（`loadCustomGamePacks()` / `saveCustomGamePacks()`）
- [x] 包导入导出（`importGamePacks()`，merge / overwrite 模式）
- [x] `prompts` → `instructions` 向后兼容
- [x] 内置包：DND 跑团基础包（`builtin_dnd_adventure`）
- [x] 内置包：COC 调查基础包（`builtin_coc_investigation`）

### 待完成

- [ ] 更多内置机制包
  - [ ] 生存类基础包（饥饿/天气/资源管理）
- [ ] 机制包版本号与升级机制
  - [ ] 包格式版本字段（`formatVersion`）
  - [ ] 包升级迁移函数
- [ ] 机制包在线分享/社区（远期）

---

## 七、GameMode — 存档与状态

> 详细设计文档：`docs/GameMode/存档与导入.md`

### 已完成

- [x] `ensureGameMetadata()`：旧存档无缝升级（`||=` 逐步补齐）
- [x] 状态面板 UI（stat / text / list 三种类型）
- [x] 移动端状态摘要条 + 抽屉面板
- [x] 消息元数据保存（gameEvent: phase/toolResults/triggerResults/changes/choices）
- [x] 回合数自增（`incrementTurn()`）
- [x] 状态快照跟随消息存档（`stateSnapshot` 嵌入每条 assistant 消息的 gameEvent）
- [x] 快照工具函数（`createStateSnapshot()` / `restoreStateFromSnapshot()` / `findLatestSnapshot()`）
- [x] Fork 分支时从目标消息快照恢复游戏状态
- [x] Undo 回退：从上一条消息快照恢复状态 + UI 回退按钮
- [x] 旧消息向后兼容（无快照时 best effort 回退到 initialState）

### 待完成

- [ ] 独立游戏存档管理
  - [ ] 多存档槽
  - [ ] 存档命名/时间戳
  - [ ] 存档导入/导出（独立于全局对话存档）
- [ ] 游戏回放功能（查看历史回合的工具结果和状态变化）

---

## 八、GameMode — 用户体验

### 已完成

- [x] 运行日志系统（级别筛选、Console 镜像、清空、持久化）
- [x] 手动工具按钮（visibility=manual 的工具显示为按钮）
- [x] 机制包选择器（内置 + 自定义分组下拉）
- [x] 机制包切换保护（有消息时禁止切换）
- [x] AI 响应容错解析（`safeParseGameResponse()`）
- [x] 对话标题自动生成
- [x] 取消请求（AbortController）
- [x] 开场消息
- [x] 消息折叠
- [x] 机制包自带加载提示（`loadingMessages`，AI 思考时主题化轮换展示）

### 待完成

- [ ] 流式输出支持
  - [ ] 研究 JSON 分段流式解析方案
  - [ ] 或改用双阶段策略：工具阶段非流式 + 叙述阶段流式
- [ ] 工具结果可视化增强
  - [ ] 骰子动画
  - [ ] 遭遇卡片式展示
  - [ ] 状态变化 diff 高亮
- [ ] 玩家选择 UI
  - [ ] AI 提供选项 → 玩家点击选择 → 结果回传
  - [ ] 支持超时自动选择

---

## 九、代码架构优化

### 待完成

- [ ] `index.vue` 逻辑拆分（当前 1353 行）
  - [ ] 提取 `gameTurnLoop.js`：回合循环、AI 调用编排
  - [ ] 提取 `gameUIState.js`：日志面板、状态面板、移动端适配的 UI 状态
  - [ ] `index.vue` 只保留模板绑定和事件分发
- [ ] 单元测试
  - [ ] `gamePackRuntime.js` 核心函数测试
  - [ ] 随机算法测试（加权、不放回、条件过滤）
  - [ ] 触发器条件判断测试
  - [ ] 机制包归一化测试
- [ ] E2E 测试
  - [ ] GameMode 基本流程（选包→发消息→工具执行→叙述返回）
