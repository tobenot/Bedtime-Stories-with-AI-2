export const changelogData = `
# 睡前故事 (Bedtime-Stories-with-AI) 更新日志

## 2026年4月
*   **安全与体验**：调整密码输入相关逻辑，优化输入框的自动填充规则。

## 2026年3月
*   **模型支持**：新增 OpenAI \`gpt-5.4\` 及 \`gpt-5.4-pro\` 模型；新增 OpenRouter 侧的 Google \`gemini-3.1-flash-lite-preview\` 模型。
*   **数据导出**：最近对话导出条数上限由 30 条调整为 80 条。
*   **界面渲染**：更新 Markdown 渲染组件。

## 2026年2月
*   **模型支持**：新增 Google \`gemini-3.1-flash-image-preview\` 模型；新增 Anthropic \`claude-opus-4.6\` 模型。
*   **底层重构**：将聊天存档存储方案由 localStorage 迁移至 IndexedDB。统一使用 UUID 作为对话和消息 ID，并按创建时间排序。
*   **数据与安全**：新增对话密码保护功能；支持加密存档的导入与导出；支持多页签存档同步与对话锁功能。
*   **核心功能**：
    *   新增对话内容搜索功能。
    *   新增长消息折叠并持久化折叠状态（支持仅展示首尾两行）。
    *   新增“发送但不回复”功能。
    *   新增消息列表中粗略的 Token 统计显示。
    *   新增导出最近对话存档及导出对话标题列表（txt格式）功能。
    *   支持消息生成中断后保留当前已生成内容。
*   **界面与交互**：
    *   放宽对话标题长度限制，支持侧边栏标题多行显示。
    *   新增标题字数统计与修改标题提醒逻辑。
    *   优化 401 权限错误提示及保存失败提示。
    *   移除“复制对话”能力；移除对话总结功能。
*   **代码清理**：下线并完全删除“虚拟恋人模式”相关代码及文档。

## 2026年1月
*   **功能**：新增当前对话单独导出功能，并优化了存档格式。
*   **界面**：优化移动端布局，增加对移动端安全区的处理；优化滚动导航逻辑。

## 2025年12月
*   **性能**：为 AI 响应处理引入节流机制，UI 更新频率限制为 1000ms，降低高频输出时的性能开销。
*   **功能**：新增画图模式（Draw mode）。

## 2025年10月
*   **底层重构**：核心架构重构为微内核模式；拆分 AppCore 方法到独立模块。
*   **功能**：引入 Key 管理器与本地模型路由（lmrouter）；支持 JSON 格式的流式输出保存与解析。
*   **界面**：新增角色选择器（Character Selector）。
*   **修复**：解决 AI 流式输出中断、死循环及 DeepSeek 调用的相关问题。

## 2025年9月
*   **模型与接口**：
    *   新增 OpenRouter 接口支持。
    *   新增 Gemini 的思考（Reasoning）模式解析。
    *   新增最大 Token 数（maxTokens）设置。
*   **界面与交互**：
    *   新增全局侧边栏。
    *   将模型选择器从主界面抽离为独立组件并整合至顶部导航栏。
    *   替换“后端代理”表述为“神秘链接”，并为其提供密码输入保护选项。
    *   缩小 Markdown 及推理内容的段落间距。
    *   禁用移动端默认的缩放行为。
*   **数据**：支持跨会话持久化当前对话 ID；合并与优化历史存档逻辑。
*   **修复**：处理 Gemini 代理的跨域问题及流式输出首帧丢失问题。

## 2025年8月
*   **版本初始化**：Bedtime-Stories-with-AI-2 仓库建立。
*   **核心**：实现前后端分离的代理模式；集成基础的 Gemini API 调用逻辑。

## 2025年3月 (v1 阶段)
*   **接口**：新增对 Ark API 的支持。
*   **工具**：新增独立的 Markdown 处理工具；新增将会话列表导出为 TXT 文件的功能。

## 2025年2月 (v1 阶段)
*   **底层重构**：项目重构为 Vite + Tailwind CSS 架构；拆分 AI 服务逻辑。
*   **核心功能**：
    *   支持对话的本地导入与导出。
    *   支持导出对话为 TXT 小说格式或 PDF 格式。
    *   新增消息的编辑、重新生成、删除及复制功能。
    *   支持本地剧本编辑器及多重提示词通配符（Prompt Wildcards）读取。
    *   新增对话标题自定义与收藏功能。
*   **界面与交互**：
    *   新增推理内容自动折叠功能。
    *   新增手动滚动至底部按钮。
    *   新增温度（Temperature）参数设置项。
    *   重新分类并按组整理功能按钮。
`;
