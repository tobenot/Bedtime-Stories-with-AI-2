export const changelogData = `
# 睡前故事 (Bedtime-Stories-with-AI) 更新日志

## 2026年9月
*   **体验优化**：预设选择框支持搜索筛选（输入预设名实时过滤），预设增多后更易查找。
*   **新增预设**：新增 ZenMux 多模型聚合网关预设（支持国内支付方式），接入邀请注册链接。单 base 同时支持 OpenAI 兼容与 Anthropic 原生协议：Claude 模型自动走原生端点，开放手动 Prompt Cache 控件；非 Claude 模型走 Chat Completions。

## 2026年8月
*   **阿里云百炼预设接入推广链接**：使用阿里云百炼预设时，设置页展示阿里云「云小站」推广链接（推荐返现计划）。
*   **绘图模式调整**：绘图模式停止新开，绘图功能迁移至独立绘图站 [image.tobenot.top](https://image.tobenot.top)（OpenAI 兼容图片生成 WebUI），已有绘图会话不受影响。
*   **OpenAI Responses API（重磅）**：
    *   接入 OpenAI Responses API（\`/v1/responses\`），成为继 \`chat_completions\`、\`anthropic_messages\` 之后的第三种请求格式驱动；遇 404/405/5xx 时自动回退到 \`chat_completions\`。
    *   支持显式切换 API 格式（\`auto | chat_completions | anthropic_messages | responses\`），并在不支持手动缓存的格式下展示缓存不可用原因。
*   **PWA 支持**：启用 PWA（vite-plugin-pwa，autoUpdate），站点可安装为应用并在后台自动更新。
*   **功能**：新增“清空全部对话”功能，可一键清理全部会话。
*   **SEO 与分享**：添加 OG 分享预览标签与占位图；更新 SEO 文案并移除过时的 gh-pages 部署脚本。
*   **修复**：
    *   修复 Anthropic 流式 \`thinking_delta\` 读错字段导致思考内容不显示的问题。
    *   区分 Claude 手动缓存与中转自动缓存的 UI 文案，避免用户混淆。
*   **新增预设**：新增 9 个官方直连预设 —— OpenAI、Anthropic、xAI (Grok)、智谱 AI、Kimi (Moonshot)、阿里云百炼、腾讯混元、百度千帆、讯飞星火。（邀请注册链接待接入。）
*   **新增预设**：新增小米 MiMo 官方预设（mimo-v2.5 系列），并接入邀请注册链接（新用户注册双方各得 ¥10 API 体验金 + 首单 9 折）。
*   **多标签页同时对话**：支持在多个浏览器标签页并行开聊不同对话，互不覆盖并实时同步（Web Locks 锁内合并 + BroadcastChannel 广播）。复制标签页时自动检测并新建对话，避免两窗口抢写同一对话。
*   **安全**：对话“密码”更名为“屏幕锁”，并明确标注其只隐藏界面、不加密内容；加密存档的导出/导入保持真实加密（AES-256-GCM）。

## 2026年7月
*   **界面与交互**：
    *   新增暗色模式与消息进入动画。
    *   交互体验优化：a11y 改进、整行可切换、Esc 取消重命名、设置页按钮分组。
    *   为 AI 消息新增快速跳到底部的工具栏按钮。
    *   消息底部显示 API 输入/缓存 token 用量。
*   **预设与邀请链接**：
    *   预设支持通用邀请注册链接（\`affiliateUrl\`）。
    *   硅基流动、火山引擎预设接入邀请注册链接；新增 OpenCode 预设并接入邀请链接。
*   **模型修复**：GPT-5/o 系列改用 \`max_completion_tokens\`，避免 400 错误。
*   **代码质量**：清理死代码、按需加载图标，修复 XSS 与失效 loading 图标。

## 2026年6月
*   **Prompt Cache 缓存机制（重磅）**：
    *   引入 prompt cache 缓存机制，支持手动缓存断点与自动缓存断点逻辑。
    *   优化自动缓存断点与交互体验；统一缓存徽章样式（自动点：白底灰字 + 灰描边，在灰按钮/白消息背景上均清晰可见）。
    *   新增 5 分钟缓存倒计时组件（\`CacheCountdown\`）。
    *   修复缓存按钮点击、缓存徽章失效、1 小时缓存失效及倒计时时间对齐等问题。
*   **Anthropic 原生调用**：接入 Anthropic 原生 \`/v1/messages\` 调用。
*   **数据迁移**：新增数据迁移功能，支持从旧版配置平滑迁移。
*   **底层重构**：路径改革，统一路径别名 \`@\` -> \`./src\`。
*   **部署修复**：修复 GitHub Pages 部署重复 artifact 与自定义域名配置问题。

## 2026年5月
*   **游戏模式（重磅）**：
    *   引入通用游戏模式运行时（Generic Game Mode Runtime），支持以"机制包"形式扩展玩法。
    *   新增机制包工具集（gamemode toolset）与机制包字段覆盖能力，支持加强字段覆盖、可见性相关字段控制。
    *   新增游戏模式日志系统与时间戳版本号。
    *   新增"做第一条对话后锁定对话模式"逻辑，避免中途切换造成上下文错乱。
    *   优化游戏模式 UI 与提示词构造（含强化文学性叙述）。
    *   新增机制包制作指南、提示词构造、开发进度等开发者文档。
*   **AI 输出与解析**：
    *   支持 \`json_schema\` → \`json_object\` → 普通提示词的分级降级，提升不同模型的兼容性。
    *   修复 \`phase=final\` 阶段 narration 中混入可选行动/选项列表的问题；将"可选行动"从正文中移除。
    *   修复模式格式不匹配时漏消息的问题。
*   **稳定性与性能**：
    *   新增前端阻塞层，长任务期间避免误操作。
    *   修复阻塞导致无法切换供应商、Key 切换失败的问题。
    *   修复频繁存储导致页面阻塞/崩溃的问题。
    *   优化 \`StandardChatMode\` 滚动性能；修复 \`TxtNovelExporter\` 缺失 import。
*   **界面与交互**：
    *   优化更新日志按钮样式。
    *   优化首条消息自动生成标题的截断逻辑（更短、不留省略号）。
    *   移动端输入区将"仅发送（不触发回复）"作为独立并排按钮显示，避免下拉菜单在触屏被挤压不可见。

## 2026年4月
*   **API 架构重构**：分阶段完成 API 模块重构（Phase 1A / 1B / 2 / 3 及多轮 Review），代码结构更清晰，便于扩展更多供应商。
*   **存档系统**：
    *   引入"冷热分离"存档方案：完整备份导出改为冷热分离结构，长期保存与日常使用解耦。
    *   归档对话列表上引入虚拟滚动，可流畅处理千条以上的归档对话。
    *   修复 IndexedDB 结构化克隆失败的问题。
*   **模型与供应商**：
    *   新增对老张 API 的支持。
    *   新增 AIHubMix 预设。
    *   支持自动拉取供应商的模型列表。
    *   新增 OpenRouter 上的 \`anthropic/claude-opus-4.7\`、\`deepseek-v4-flash\`、\`deepseek-v4-pro\` 模型，并恢复 Gemini 3.1 系列在 OpenRouter 列表中的展示。
    *   优化 OpenRouter 模型列表的增删与 DrawMode 默认模型。
*   **安全与体验**：调整密码输入相关逻辑，优化输入框的自动填充规则。
*   **界面**：模型选择器字体放大；新增更新日志入口。

## 2026年3月
*   **模型支持**：新增 OpenAI \`gpt-5.4\` 及 \`gpt-5.4-pro\` 模型；新增 OpenRouter 侧的 Google \`gemini-3.1-flash-lite-preview\` 模型。
*   **数据导出**：最近对话导出条数上限由 30 条调整为 80 条。
*   **界面渲染**：更新 Markdown 渲染组件。

## 2026年2月
*   **模型支持**：新增 Google \`gemini-3.1-flash-image-preview\` 模型；新增 Anthropic \`claude-opus-4.6\` 模型。
*   **底层重构**：将聊天存档存储方案由 localStorage 迁移至 IndexedDB。统一使用 UUID 作为对话和消息 ID，并按创建时间排序。
*   **数据与安全**：新增对话密码保护功能；支持加密存档的导入与导出。
*   **核心功能**：
    *   新增对话内容搜索功能。
    *   新增长消息折叠并持久化折叠状态（支持仅展示首尾两行）。
    *   新增“发送但不回复”功能。
    *   新增消息列表中粗略的 Token 统计显示。
    *   新增导出最近对话存档及导出对话标题列表（txt格式）功能。
    *   支持消息生成中断后保留当前已生成内容。
*   **界面与交互**：
    *   放宽对话标题长度限制，支持侧边栏标题多行显示。
    *   新增标题字数统计与修改标题提醒逻辑。
    *   优化 401 权限错误提示及保存失败提示。
    *   移除“复制对话”能力；移除对话总结功能。
*   **代码清理**：下线并完全删除“虚拟恋人模式”相关代码及文档。

## 2026年1月
*   **功能**：新增当前对话单独导出功能，并优化了存档格式。
*   **界面**：优化移动端布局，增加对移动端安全区的处理；优化滚动导航逻辑。

## 2025年12月
*   **性能**：为 AI 响应处理引入节流机制，UI 更新频率限制为 1000ms，降低高频输出时的性能开销。
*   **功能**：新增画图模式（Draw mode）。

## 2025年10月
*   **底层重构**：核心架构重构为微内核模式；拆分 AppCore 方法到独立模块。
*   **功能**：引入 Key 管理器与本地模型路由（lmrouter）；支持 JSON 格式的流式输出保存与解析。
*   **界面**：新增角色选择器（Character Selector）。
*   **修复**：解决 AI 流式输出中断、死循环及 DeepSeek 调用的相关问题。

## 2025年9月
*   **模型与接口**：
    *   新增 OpenRouter 接口支持。
    *   新增 Gemini 的思考（Reasoning）模式解析。
    *   新增最大 Token 数（maxTokens）设置。
*   **界面与交互**：
    *   新增全局侧边栏。
    *   将模型选择器从主界面抽离为独立组件并整合至顶部导航栏。
    *   替换“后端代理”表述为“神秘链接”，并为其提供密码输入保护选项。
    *   缩小 Markdown 及推理内容的段落间距。
    *   禁用移动端默认的缩放行为。
*   **数据**：支持跨会话持久化当前对话 ID；合并与优化历史存档逻辑。
*   **修复**：处理 Gemini 代理的跨域问题及流式输出首帧丢失问题。

## 2025年8月
*   **版本初始化**：Bedtime-Stories-with-AI-2 仓库建立。
*   **核心**：实现前后端分离的代理模式；集成基础的 Gemini API 调用逻辑。

## 2025年3月 (v1 阶段)
*   **接口**：新增对 Ark API 的支持。
*   **工具**：新增独立的 Markdown 处理工具；新增将会话列表导出为 TXT 文件的功能。

## 2025年2月 (v1 阶段)
*   **底层重构**：项目重构为 Vite + Tailwind CSS 架构；拆分 AI 服务逻辑。
*   **核心功能**：
    *   支持对话的本地导入与导出。
    *   支持导出对话为 TXT 小说格式或 PDF 格式。
    *   新增消息的编辑、重新生成、删除及复制功能。
    *   支持本地剧本编辑器及多重提示词通配符（Prompt Wildcards）读取。
    *   新增对话标题自定义与收藏功能。
*   **界面与交互**：
    *   新增推理内容自动折叠功能。
    *   新增手动滚动至底部按钮。
    *   新增温度（Temperature）参数设置项。
    *   重新分类并按组整理功能按钮。
`;
